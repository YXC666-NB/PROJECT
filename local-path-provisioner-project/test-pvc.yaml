apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-pvc
  namespace: test
spec:
  storageClassName: local-storage-sc
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
