apiVersion: v1
kind: Service
metadata:
  name: redis-svc
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "biz-db-chart.labels" . | indent 4 }}
spec:
  selector:
    app: redis
  clusterIP: None
  ports:
  - port: 6379
    targetPort: 6379
    name: redis
