apiVersion: v1
kind: Service
metadata:
  name: jenkins-svc
  namespace: ci
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30190
    name: http
  - port: 50000
    targetPort: 50000
    nodePort: 30091
    name: agent
  selector:
    app: jenkins
